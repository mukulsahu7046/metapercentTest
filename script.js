fetch("./class.json").then((res)=>res.json()).then((data)=>console.log(data));

const temp= `<img src="${dt.ImageUrl}">
				<h1>${dt.ID}</h1>
				<p>${dt.ShortDesc}</p>
				<button>view</button>`
data.map(function(document.getElementById("grid").innerHTML=`${temp}`));